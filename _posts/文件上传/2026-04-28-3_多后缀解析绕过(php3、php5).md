---
title: 文件上传 | 多后缀解析绕过
date: 2026-04-28 22:00:00 +0800
categories: [笔记]
tags: [文件上传]
---
## 题目
---

```php
$deny_ext = array('.asp', '.aspx', '.php', '.jsp');

$file_name = trim($_FILES['upload_file']['name']);
$file_name = deldot($file_name); // 删除文件名末尾的点
$file_ext = strrchr($file_name, '.'); // 以最后一个点作为分割，获取文件后缀
$file_ext = str_ireplace('::$DATA', '', $file_ext); // 去除字符串::$DATA
$file_ext = trim($file_ext); // 首尾去空

if (!in_array($file_ext, $deny_ext)) {
    $temp_file = $_FILES['upload_file']['tmp_name'];
    $img_path = UPLOAD_PATH . '/' . date("YmdHis") . rand(1000, 9999) . $file_ext;
    if (move_uploaded_file($temp_file, $img_path)) {
        $is_upload = true;
    } else {
        $msg = '上传出错';
    }
} else {
    $msg = '不允许上传.asp, .aspx, .php, .jsp后缀文件';
}
```
### 分析

黑名单只拦截了 `.asp`、`.aspx`、`.php`、`.jsp`，但 PHP 还存在其他可执行后缀（如 `.php3`、`.php5`、`.phtml`），不在黑名单内。

> 注意：这些后缀默认不被 Apache 当作 PHP 解析，需要手动配置 `httpd.conf`。

## HOW
---

### 环境准备

本关基于小皮面板（phpStudy）以 Apache 作为中间件搭建靶场

1. 打开 Apache 配置文件 `httpd.conf`
2. 找到 `AddType application/x-httpd-php .php`，取消注释
3. 在行末追加 `.php3 .php5 .phtml`：

```apache
AddType application/x-httpd-php .php .php3 .php5 .phtml
```

4. 切换 PHP 版本为 **php-5.4.45 + Apache**
5. 重启 phpStudy

### 实操流程

1. 上传 `111.php` 的一句话木马
2. BP 抓包，将 `filename` 修改为 `111.php3`，放行发送
3. 上传成功后，在新标签页打开图片，获取 URL
4. 复制 URL 至蚁剑连接即可


## 补充：PHP 线程安全版本区别
---

|类型|全称|说明|
|---|---|---|
|**NTS**|Non Thread Safe|非多线程安全|
|**TS**|Thread Safe|多线程安全，较主流|